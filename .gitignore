# OS
.DS_Store

# Schedule template spreadsheets — keep out of the repo
*.xlsx

# Standalone HTML prototypes — superseded by deploy/static/index.html (the served app)
/index.html
/mcltemplatebuilder.html
/mcltemplatebuilder_v2.html

# Python
__pycache__/
*.pyc

# Local app/runtime data — schedule lives in the Docker volume, not the repo
deploy/data/
*.db
*.sqlite
*.sqlite3

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
